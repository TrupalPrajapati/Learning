// Write a program that takes a string as input and prints it in uppercase.

let sentence = prompt("enter the sentece");

let sentence1 = (sentence.toUpperCase(sentence));
console.log("sentence is in the capital mode : ",sentence1);